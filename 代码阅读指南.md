# skynet-mcp-client（对话式 MCP 客户端）代码阅读指南

本工程是面向 skynet 游戏服务器的**对话式 MCP 图形化客户端**（非 coding agent）：用户用自然语言直接控制 skynet debug console，LLM 决策调用哪个 MCP 工具，危险命令弹审批卡人工确认，结果在对话里渲染成表格/图表，所有调用落库可审计、会话可保存回放。

规模不大但链路清晰：后端 Python 11 个文件（FastAPI + LangGraph + MCP SDK + SQLite）、前端 React + Vite + TS + Tailwind。建议按「入口 → 数据流 → 核心机制」的顺序读。

## 主干数据流

```
浏览器 (React + Vite + Tailwind + ECharts)
   │  SSE：POST /api/chat、POST /api/human-feedback
   ▼
FastAPI 后端（backend/app.py → backend/routes.py）
   ├─ get_agent() 懒加载：McpConnection → list_tools → build_langchain_tools → SkynetAgent
   ├─ graph.astream(stream_mode=["messages","updates"]) → SSE 事件流
   │     call_model（LLM 决策）→ execute_tools（危险工具 interrupt 审批）
   ▼
skynet-mcp server（Linux 内，HTTP 模式 :8765）  ← MCP streamable-http
   ▼
skynet debug console (127.0.0.1:8000)
```

其中 LangGraph 节点链是核心骨架：

```
call_model（LLM bind_tools 产出 AIMessage，可能带 tool_calls）
  → should_continue（条件边：有 tool_calls → execute_tools；否则 END）
  → execute_tools
      ├─ 任一危险调用（medium/high）→ interrupt() 挂起，SSE 下发 human_approval 审批卡
      │     → 用户 approve → 执行全部工具 → ToolMessage 回填 → 回 call_model
      │     → 用户 reject  → 注入"已拒绝"说明 → 回 call_model
      └─ 全部 safe → 直接执行 → ToolMessage 回填 → 回 call_model
  → END（LLM 基于工具真实结果生成最终中文回复）
```

## 推荐阅读顺序

### 第 0 步：先建立整体认识

- 读 `README.md` 的「架构图」和「API」表，理解整条链路的形状。
- 读 `backend/config.py` —— 配置全在 `.env`（复制 `.env.example`）：`MCP_URL`（默认 `http://127.0.0.1:8765/mcp`）、`CLIENT_HTTP_PORT`（8100）、LLM 三件套（不配则对话不可用，但状态/工具接口可用）。
- 读 `backend/state.py`（只有 10 行）—— 全局共享单例 `settings` / `mcp_conn` / `db`，是避免 app ↔ routes 循环导入的"共享实例"层。

### 第 1 步：`backend/app.py` 与 API 层（真正入口）

- `app.py:19` 的 `lifespan` 启动时连接 MCP（失败不阻塞，`/api/status` 会显示未连接）、关闭时释放连接与 DB。
- `app.py:32-42`：注册路由 + 静态托管前端 `dist`（同源部署，无 CORS；没有 dist 时回退托管 `frontend/` 源码目录）。
- 跳 `backend/routes.py`：
  - `GET /api/status`（:34）/ `GET /api/tools`（:57）—— 连接状态、32 个工具列表（含危险等级，前端顶栏和测试用）。
  - `get_agent()`（:81）—— 懒加载单例：连接 MCP → `list_tools()` → `build_langchain_tools()` → `SkynetAgent`，加锁防并发重复构建。
  - `POST /api/chat`（:250）—— 创建/复用会话（标题取首条消息前 24 字符）、落库用户消息、SSE 流式返回。
  - `POST /api/human-feedback`（:265）—— 危险命令审批：`Command(resume=...)` 从 interrupt 断点恢复。
  - 会话历史（`/api/sessions` 系列）、调用审计（`/api/calls`、`/api/audit`）。

### 第 2 步：`backend/routes.py` 的 `_emit_stream`（SSE 事件流核心，最容易踩坑）

`_emit_stream`（:191）驱动 `graph.astream`，把 LangGraph 输出转成 SSE 事件，这里集中了本项目几个关键设计：

- **progress 只推 AI token**：`stream_mode="messages"` 会流式产出所有消息（含 ToolMessage 工具原始输出），代码用 `msg.type == "ai"` 过滤——否则 help 的英文原始表格会混进对话流。
- **interrupt 挂起时不发 result**：`not gs.next and not values.get("__interrupt__")` 时才 yield result；否则前端收到 result 会把审批卡清掉（`pending: null`）。等用户审批 resume 后由下一轮 `_emit_stream` 出结果。
- **不传原始表格 render**：`_record_tool_calls` 只做落库（calls/messages），`last_render` 恒为 None——前端统一由 LLM 翻译的中文 markdown 表格呈现，避免和 ResultTable 重复。
- **OUT_OF_SCOPE_HINT 兜底**：本次执行没有任何 ToolMessage（LLM 违规自由发挥）时，把最终文本替换为固定提示，保证用户只看到工具结果或提示。

### 第 3 步：`backend/agent.py`（LangGraph 编排核心，最重要）

- `SYSTEM_PROMPT`（:27 起，f-string）—— 硬性规则：只能通过工具完成请求、禁止编造命令输出、无关请求回复固定提示（`OUT_OF_SCOPE_HINT`）、英文说明翻译成中文、分组表格前加 `**章节名**` 标题。
- `_build_graph`（:81）：三个可执行单元 + 一条条件边：
  - `call_model`（:82）—— 组装 `SystemMessage + 历史`，`ainvoke` 绑定工具的模型。
  - `should_continue`（:87）—— 最后一条消息带 `tool_calls` 走 `execute_tools`，否则 END。
  - `execute_tools`（:91）—— 先算每个调用的危险等级，**任一危险 → `interrupt()` 挂起**（HITL）；approve 后批量执行、reject 注入拒绝说明；全部安全则直接执行并回填 ToolMessage。
- `_danger_of`（:66）—— `raw_command` 按命令行首词细化危险度，其余按 danger_map 查表。
- 关键理解：`interrupt()` 依赖 checkpointer（`MemorySaver`）+ `thread_id`（=session_id）标识会话，审批 resume 从断点继续。

### 第 4 步：`backend/mcp_client.py`（连接层）

- `connect()`（:40）—— streamable-http 建立会话并 `initialize`，幂等（已连则跳过）；read 超时调大到 120s（stat/mem 广播全服慢）。
- `list_tools()`（:92）—— 缓存工具元数据，失败自动重连一次。
- `call_tool()`（:114）—— 调用并归一化为 `{ok, text}`（MCP 的 `isError` 不是异常）；断连自动重连重试。

### 第 5 步：`tools_adapter.py` + `danger.py` + `parsers.py`（三个纯逻辑模块）

- `tools_adapter.py:86` `build_langchain_tools` —— MCP 的 JSON Schema 动态转 Pydantic 模型（`json_schema_to_model`，enum→Literal、非必填→None 默认），包成 `StructuredTool`，并返回工具名→危险等级映射。
- `danger.py` —— 危险度三档：description 含 `【危险】`→high、`⚠`→medium、其他 safe；`raw_command` 按首词细化（kill/exit/signal/inject/call→high，start/log/snax...→medium）。
- `parsers.py` —— 工具输出文本 → `{columns, rows}` 结构化（曾是前端 ResultTable 的数据源；现在前端统一走 LLM markdown 呈现，parsers 仍是 `_record_tool_calls` 审计落库的解析依据）。支持 tab 分隔与**标准 markdown 表格**（`_markdown_table` 识别 `|---|---|` 和 `+----+` 分隔线）。

### 第 6 步：`backend/db.py`（SQLite，WAL）

- 三张表：`sessions`（id/title/时间戳）、`messages`、`calls`（工具调用审计：工具/参数/危险等级/审批结果/状态）。
- `delete_session`（:96）级联删 messages/calls；`touch_session` 随消息更新 `updated_at`（会话列表按它倒序）。

### 第 7 步：前端（最后读）

- `frontend/src/lib/agentApi.ts` —— SSE 客户端：`fetch` 读流、按 `\n\n` 拆帧、正则取 `data:`、`JSON.parse`；`streamChat` / `streamHumanFeedback` / `listSessions` / `fetchSession` / `deleteSession` / `fetchStatus`。
- `frontend/src/App.tsx` —— 状态机核心：`handleEvent` 按事件类型更新最后一条消息（progress 追加 content / human_approval 挂审批卡 / result 收尾 / error 显示）；`handleSend` 立即插入"处理中"占位消息；会话列表在 `session_created` 后刷新、点击旧会话 `fetchSession` 加载历史。
- `frontend/src/components/`：
  - `MessageBubble` —— 渲染一条消息：content 用 `MarkdownMessage` 渲染、审批卡、错误条。
  - `MarkdownMessage` —— 自写轻量 markdown 渲染（表格 + `**bold**`/`` `code` ``，React 元素构建、无 XSS）。
  - `HumanApprovalCard` —— 危险命令审批卡（批准执行/拒绝）。
  - `ResultTable` / `ResultChart` —— 结构化表格/图表（保留组件，当前 render 恒 None 暂不触发）。
  - `SessionList` —— 历史会话列表（标题/时间/悬停删除）。
  - `Composer` —— 输入框（Enter 发送、Shift+Enter 换行）。

### 第 8 步：`tests/`（验证行为）

- `test_parsers.py` —— 表格解析单测（含 markdown 表格）。
- `test_agent_graph.py` —— LangGraph 行为：正常调用流、危险 interrupt approve/reject、LLM 自由发挥被替换为提示、help 抑制 render、progress 不泄漏工具输出、interrupt 挂起不发 result。
- `test_api_e2e.py` / `test_server_smoke.py` —— 端到端（依赖 skynet-mcp 仓库的 mock console，可跳过）。

## 阅读姿势建议

1. **对照 git log 读**：`git log --oneline -20`，提交历史就是这版 README 演进的过程（去 WSL 化 → uv 环境 → 表格渲染 → HITL 修复）。
2. **边读边画依赖图**：

   ```
   app.py → state.py（settings/mcp_conn/db）+ routes.py
   routes.py → agent.py（SkynetAgent）→ mcp_client.py（McpConnection）
   routes.py → tools_adapter.py + danger.py + parsers.py + db.py
   agent.py → danger.py（危险判定）+ config.py（LLM 三件套）
   所有模块 → config.py（.env 配置）
   ```

3. **找扩展点**：
   - skynet-mcp 新增工具 → 客户端零改动自动适配（schema 动态转模型），只需在 `danger.py` 按描述/首词确认危险度。
   - 想改 LLM 行为 → 改 `agent.py` 的 `SYSTEM_PROMPT`（只调工具/翻译/分组标题）。
   - 想加 SSE 事件类型 → `routes.py` 的 `_emit_stream` + 前端 `handleEvent` 同步加。
4. **用 curl 直接看 SSE 链路**（最快调试法）：

   ```bash
   curl -N -X POST http://127.0.0.1:8100/api/chat \
     -H "Content-Type: application/json" \
     -d '{"message":"列出所有服务"}'
   ```

## 最快路径

环境就绪后（Linux 手动拉起 skynet-mcp → `uv venv --python 3.12 .venv` → `uv pip install -r requirements.txt` → `.venv\Scripts\python.exe -m uvicorn backend.app:app --host 127.0.0.1 --port 8100`），打开 `http://127.0.0.1:8100` 发一条"列出所有服务"，观察：左侧出现新会话（标题=首条消息）→ 输入框显示"处理中…" → 出现中文 markdown 表格 → 落库可审计；再发"杀掉 watchdog"看审批卡。然后顺着 `routes.py → agent.py → mcp_client.py` 把链路读通，最后补前端和 parsers。
